{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^8.1.3",
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "start": "cd server && npm install && npm knex migrate:latest --env production && npm knex seed:make --env production && npm start",
    "start-client": "cd client && npm install && npm start",
    "dev": "nodemon ./src/index.js",
    "lint": "eslint --fix --ext .js .",
    "test": "mocha test && yarn lint"
  }
}
