{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^8.1.3",
    "nodemon": "^2.0.4"
  },
  "scripts": {
    "start": "cd server && yarn install && node yarn start",
    "start-client": "cd client && yarn install && yarn start",
    "dev": "nodemon ./src/index.js",
    "lint": "eslint --fix --ext .js .",
    "test": "mocha test && yarn lint"
  }
}
