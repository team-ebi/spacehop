{
    "name": "server",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
      "@stripe/react-stripe-js": "^1.1.2",
      "@stripe/stripe-js": "^1.9.0",
      "@testing-library/jest-dom": "^5.11.4",
      "dotenv": "^8.2.0",
      "express": "^4.17.1",
      "pg": "^8.4.1"
    },
    "devDependencies": {
      "chai": "^4.2.0",
      "chai-http": "^4.3.0",
      "mocha": "^8.1.3",
      "nodemon": "^2.0.4"
    },
    "scripts": {
      "start":"cd server && npm install && node ./src/index.js",
      "start:server": "cd server && npm install && npm start",
      "start:client": "cd client && npm install && npm run build",
      "dev": "nodemon ./src/index.js",
      "lint": "eslint --fix --ext .js .",
      "test": "mocha test && yarn lint"
    }
  }
